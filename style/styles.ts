import { StyleSheet } from "react-native";

// Styles for text that was being repeated
export const textStyles = (textColor: string) =>
  StyleSheet.create({
    text: {
      color: textColor,
    },
  });
